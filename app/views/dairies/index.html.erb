<p id="notice"><%= notice %></p>

<h1>日記一覧画面</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>タイトル</th>
      <th>登録日時</th>
      <th>更新日時</th>
    </tr>
  </thead>

  <tbody>
    <% @dairies.each do |dairy| %>
      <tr>
        <td><%= dairy.id %></td>
        <td><%= link_to dairy.title,dairy %></td>
        <td><%= dairy.created_at %></td>
        <td><%= dairy.updated_at %></td>
<!--         <td><%= link_to 'Show', dairy %></td>
        <td><%= link_to 'Edit', edit_dairy_path(dairy) %></td>
        <td><%= link_to 'Destroy', dairy, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '日付を新規作成', new_dairy_path %>
